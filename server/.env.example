# Local development (`npm run dev`) against the MySQL container from docker compose
PORT=4000
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=inventory
DB_PASSWORD=inventory
DB_NAME=inventory
JWT_SECRET=dev-only-secret-change-me
CORS_ORIGINS=http://localhost:3000
